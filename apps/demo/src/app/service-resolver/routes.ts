import { Routes } from "@angular/router";
import { ServiceResolverComponent } from "./service-resolver.component";

export const serviceResolverRoutes: Routes = [
    {
        path: '',
        component: ServiceResolverComponent,
    }
];